name: XenonBrain Continuous Evolution & Dashboard

on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write
  issues: write
  pages: write
  id-token: write

jobs:
  train_and_deploy:
    runs-on: ubuntu-latest
    steps:
    - name: 🧬 Accessing Xenon Core
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
      
    - name: 🐍 Preparing Neural Environment
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        
    - name: 📦 Force Installing Synaptic Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install torch numpy pandas requests feedparser yfinance sentence-transformers matplotlib pyyaml tqdm scikit-learn
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: 🧠 Executing Deep Logic Training (V5 Evolution)
      run: python src/training/train.py
      env:
        PYTHONPATH: .

    - name: 📧 Sending Daily Intelligence Report
      run: python src/utils/email_notifier.py
      env:
        PYTHONPATH: .
        SENDER_EMAIL: ${{ secrets.SENDER_EMAIL }}
        SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
        
    - name: 🔔 Checking Logic Shifts
      run: python src/utils/notifier.py
      env:
        PYTHONPATH: .
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        
    - name: 🚀 Committing Evolution & Deploying Dashboard
      run: |
        if [ -f ALERT.txt ]; then
          gh issue create --title "🚨 XenonBrain: Logic Shift Detected" --body-file ALERT.txt
        fi
        git config --local user.email "action@github.com"
        git config --local user.name "XenonBrain Core"
        git add models/xenon_brain_latest.pth DAILY_REPORT.md HISTORY.json docs/
        git pull origin main --rebase --autostash
        git commit -m "🧠 Evolution V5: Sentiment Core & Email Reporting [$(date +'%Y-%m-%d')]" || echo "No changes"
        git push origin main
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 🌐 Uploading Dashboard Artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: './docs'

  deploy_pages:
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    needs: train_and_deploy
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
